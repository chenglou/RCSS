{
  "name": "examples",
  "version": "0.0.0",
  "description": "`buttons.js` contains the css directly lifted from [Bootstrap](http://getbootstrap.com), and transformed into simple JavaScript objects.",
  "main": "index.js",
  "scripts": {
    "build": "./node_modules/.bin/browserify index.jsx > out.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "chenglou",
  "license": "MIT",
  "dependencies": {
    "react": "^0.12.2"
  },
  "devDependencies": {
    "browserify": "^7.0.3",
    "reactify": "^0.17.1"
  },
  "browserify": {
    "transform": [
      [
        "reactify"
      ]
    ]
  }
}
